- insert:
    - id: browserskill
      # Must be the published package name — the loader imports this specifier.
      # Quoted because YAML reserves a leading '@' in plain scalars.
      name: "@916938/zenx-bridge-dsh-plugin"
